<h1>Create New Blog Post</h1>

<%=link_to("Back to Home Page", root_path) %>


<%= form_with(model: @new_blog, local: true) do |f|%>

<% if @new_blog.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@new_blog.errors.count, "error") %> prohibited this article from being saved:</h2>
 
    <ul>
    <% @new_blog.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

  <%= render 'form', f: f %>

  <%=f.submit "Create New Post"%>


<%end%>