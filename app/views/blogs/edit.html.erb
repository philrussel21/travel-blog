<h1>Edit Blog Post</h1>

<%=link_to("Back to Blog Page", blog_path(@edit_blog)) %>


<%= form_with(model: @edit_blog, local: true) do |f|%>

<% if @edit_blog.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@edit_blog.errors.count, "error") %> prohibited this article from being saved:</h2>
 
    <ul>
    <% @edit_blog.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>


  <%= render 'form', f: f %>

  <%=f.submit "Update Post"%>


<%end%>